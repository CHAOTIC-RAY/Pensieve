rules_version = '2';
// Triggering update
service cloud.firestore {
  match /databases/{database}/documents {

    match /{document=**} {
      allow read, write: if false;
    }

    match /mind-items/{itemId} {
      allow read, write: if true;
    }
  }
}
